#find
void LaunchMapScreen()
{
	if(procInterfacePrepare(INTERFACE_MAP))
	{
		nPrevInterface = -1;
		CurrentInterface = INTERFACE_MAP;
		InitInterface(Interfaces[CurrentInterface].IniFile);
	}
}

void LaunchPaperMapScreen()
#replacewith
bool DirectSail_PrepareStormInterface()
{
	int i;
	int nWME;
	bool bTornado;
	aref WME;
	aref rStorm;

	// Storm interception marks the persistent encounter for deletion before
	// LaunchDirSailEnc() is called. Detect that marker here, BEFORE
	// procInterfacePrepare() changes interface/world-map state. This is the
	// reliable pre-launch boundary that the old Workshop storm dialogue had.
	if(!CheckCharacterItem(pchar, "stop_storm")) return false;

	makearef(WME, worldMap.encounters);
	nWME = GetAttributesNum(WME);

	for(i = 0; i < nWME; i++)
	{
		rStorm = GetAttributeN(WME, i);

		if(!CheckAttribute(rStorm, "type")) continue;
		if(rStorm.type != "Storm") continue;
		if(!CheckAttribute(rStorm, "needDelete")) continue;
		if(rStorm.needDelete != "Reload delete storm encounter") continue;
		if(CheckAttribute(rStorm, "dsStormPromptLaunched")) continue;

		bTornado = false;
		if(CheckAttribute(rStorm, "isTornado") && sti(rStorm.isTornado) != 0)
		{
			bTornado = true;
		}

		// One-shot transport into the dedicated storm interface. The encounter
		// marker prevents a second ordinary Watchman launch from mistaking the
		// already-consumed storm for a fresh storm prompt before cleanup runs.
		rStorm.dsStormPromptLaunched = true;
		pchar.DirectSail.StormPrompt = 1;
		if(bTornado)
			pchar.DirectSail.StormPromptTornado = 1;
		else
			pchar.DirectSail.StormPromptTornado = 0;

		trace("DS STORM DIVERTER PRELAUNCH: tornado=" + bTornado);
		return true;
	}

	return false;
}

void LaunchMapScreen()
{
	Interfaces[INTERFACE_MAP].SectionName = "interface\\map.c";

	if(procInterfacePrepare(INTERFACE_MAP))
	{
		nPrevInterface = -1;
		CurrentInterface = INTERFACE_MAP;
		InitInterface(Interfaces[CurrentInterface].IniFile);
	}
}

void LaunchDirSailEnc()
{
	// Keep ordinary/quest Watchman encounters on their existing interface.
	// Only a storm already marked by CheckIslandChange is routed to the
	// dedicated two-button storm interface.
	if(DirectSail_PrepareStormInterface())
		Interfaces[INTERFACE_MAP].SectionName = "interface\\DirSailStormEnc.c";
	else
		Interfaces[INTERFACE_MAP].SectionName = "interface\\DirSailEnc.c";

	if(procInterfacePrepare(INTERFACE_MAP))
	{
		nPrevInterface = -1;
		CurrentInterface = INTERFACE_MAP;
		InitInterface(Interfaces[CurrentInterface].IniFile);
	}
}

void LaunchPaperMapScreen()
#endreplace

#find
void NewAutoSave()
{
	string sTag = GetEventData();
	if(bAutoSaveStarted)
#replacewith
void NewAutoSave()
{
	string sTag = GetEventData();

	// A Direct Sail synthetic MapEnter interruption returns from Ship_deck
	// without a native world-map entity behind it. CLAOP requests a MapToSea
	// autosave after the deck has unloaded but before AISea is constructed.
	// Saving in that gap creates an unloadable save with no valid world context.
	// Skip exactly that one autosave and run the normal after-save continuation.
	if(sTag == "MapToSea" && CheckAttribute(pchar, "DirectSail.MapEnterInterruptReturn"))
	{
		trace("DS MAPENTER AUTOSAVE SKIP: type=MapToSea");
		DeleteAttribute(pchar, "DirectSail.MapEnterInterruptReturn");
		AutoSave_After();
		return;
	}

	if(bAutoSaveStarted)
#endreplace
