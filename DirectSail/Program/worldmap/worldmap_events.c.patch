#find
			if(at.type == "battle")
			{
				//wdmQuestCreateBattle(at.characterID, sti(at.iEnemyNation), sti(at.TimeOut));
			}
		}
	}
#replacewith
			if(at.type == "battle")
			{
				//wdmQuestCreateBattle(at.characterID, sti(at.iEnemyNation), sti(at.TimeOut));
			}

			// Direct Sail can return a partially-aged quest encounter to the
			// native queue. Map_Create* only accepts whole timeout days, so after
			// the native encounter exists restore the exact remaining lifetime.
			if(CheckAttribute(at, "DirectSailRemainingLife"))
			{
				if(encID != "")
				{
					encPath = "encounters." + encID;
					if(CheckAttribute(&worldMap, encPath))
					{
						worldMap.(encPath).livetime = stf(at.DirectSailRemainingLife);
						trace("DS QUEST WORLD MAP LIFETIME RESTORED: chr=" + at.characterID +
						      " id=" + encID +
						      " life=" + at.DirectSailRemainingLife);
					}
				}
			}
		}
	}
#endreplace
