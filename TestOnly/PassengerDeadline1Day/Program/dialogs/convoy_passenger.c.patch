#find
	npchar.GenQuest.GetPassenger_Time = makeint(sti(daysQty) * (frand(1.3) + 0.7));
#replacewith
	// TEST ONLY: force the first passenger deadline to one day so the
	// ConvoyMapPassenger -> MapEnter -> AllPassengerDeck path can be reproduced
	// quickly and deterministically.  Do not ship this with a release build.
	npchar.GenQuest.GetPassenger_Time = 1;
	trace("DS TEST PASSENGER: initial deadline forced to 1 day");
#endreplace
